// import {createUrl} from './index.js'

// describe('createUrl', () => {
//   test('should returns a URL based on the template and replaces all `{name}` with params.name', () => {
//       expect(createUrl('/api/{id}', {id: 0})).toBe('/api/0');
//   });
//   test('should return undefined when pass does not found', () => {
//     expect(createUrl('/api/{id}', {name: 'Petya'})).toBe('/api/undefined');
//   });
//   test('should rreturns a URL based on the template and replaces all `{name}` with params.name', () => {
//     expect(createUrl('/api/{list}/{id}', {list: 'items', id: 0})).toBe('/api/items/0');
//   });
// });
